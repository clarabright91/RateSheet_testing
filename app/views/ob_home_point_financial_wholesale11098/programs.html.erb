<div class="container">
  <div class="row col-md-6 col-md-offset-2 custyle">
  <table class="table table-striped custab">
  <thead>
  <h2><%= @sheet_obj.name.capitalize + " Programs List" %></h2>
  <tr>
    <th>No.</th>
    <th>Name</th>
    <th class="text-center">Action</th>
  </tr>
  </thead>
    <% @programs.each_with_index do |program, index|%>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= program.program_name %></td>
        <td class="text-center"><a class='btn btn-info btn-xs' href=<%= "/ob_home_point_financial_wholesale11098/"+ program.id.to_s + "/single_program" %>><span class="glyphicon glyphicon-edit"></span> View</a></td>
      </tr>
    <%end%>
  </table>
  </div>
</div>